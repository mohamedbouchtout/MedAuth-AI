"""HTTP surface for track-b-rag.

The response envelope and error handlers live in the shared
``api_envelope`` package, not here — every service returns the same two
shapes and one definition of them is the point.
"""
