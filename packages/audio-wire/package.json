{
  "name": "@medauth/audio-wire",
  "version": "0.1.0",
  "private": true,
  "description": "The encounter-audio wire format shared by apps/mobile and apps/web — 16kHz mono int16 LE, 250ms frames, and the failure vocabulary that goes with it.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
