{
  "name": "medauth-ai",
  "version": "0.1.0",
  "private": true,
  "description": "MedAuth AI — ambient clinical AI platform. JS/TS workspaces (frontends only; all services are Python).",
  "workspaces": [
    "apps/web",
    "apps/mobile",
    "packages/audio-wire",
    "packages/session-client",
    "packages/fhir-types/typescript"
  ],
  "engines": {
    "node": ">=24.15"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  },
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "dev:mobile": "npm run start --workspace apps/mobile",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  }
}
